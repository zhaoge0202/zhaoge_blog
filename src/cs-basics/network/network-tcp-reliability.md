---
title: "TCP 如何保证可靠传输？"
description: "从序列号、ACK、校验、重传和 SACK 讲清 TCP 可靠字节流。"
breadcrumb: true
article: true
editLink: false
category:
  - "计算机网络"
tag:
  - "高频"
  - "原理深入"
  - "体系化"
prev:
  text: "TCP 四次挥手为什么需要 TIME_WAIT？"
  link: "/cs-basics/network/network-tcp-four-way-wave-time-wait.html"
next:
  text: "滑动窗口、拥塞控制、流量控制分别解决什么问题？"
  link: "/cs-basics/network/network-tcp-flow-congestion-control.html"
---

# TCP 如何保证可靠传输？

> TCP 不是让网络不丢包，而是在不可靠的 IP 之上，用编号、确认、重传、排序去重和窗口机制拼出可靠字节流。

## 先纠正一个说法：可靠的是字节流

TCP 面向字节流，不保留应用层消息边界。应用写入的两次数据，接收方可能一次读到；应用一次写入的数据，接收方也可能多次读到。

这就是 Java 网络编程里常说的“粘包/拆包”根源：TCP 只保证字节按序交付，不知道你的业务消息从哪里开始、到哪里结束。HTTP、MySQL 协议、RPC 协议都要自己定义消息边界，比如长度字段、分隔符或报文格式。

所以“可靠传输”不要被理解成“业务请求一定成功”。TCP 能把连接内已确认的数据尽力按序交付，但它不知道一次下单、一次支付、一次 RPC 调用是否已经被业务处理。连接断开、超时、服务端处理后响应丢失这些场景，仍然要靠应用层超时、重试、幂等和事务边界兜底。

## TCP 可靠性靠哪些机制？

| 机制           | 解决的问题                               |
| -------------- | ---------------------------------------- |
| 序列号         | 标识每段字节在流中的位置，解决乱序和重复 |
| ACK 确认       | 告诉发送方哪些字节已经收到               |
| 校验和         | 发现传输过程中的常见比特错误             |
| 重传           | 丢包或 ACK 丢失时重新发送数据            |
| 接收端排序去重 | 乱序到达先缓存，重复到达直接丢弃         |
| 流量控制       | 避免把接收方缓冲区打满                   |
| 拥塞控制       | 避免把网络打爆                           |

这几类机制配合起来，应用层看到的才是有序、无重复的字节流。

## ACK 为什么能确认“之前的数据都收到了”？

TCP 常用累计确认。假设接收方期待的下一个字节序号是 5000，它回 `ACK=5000`，含义是：

> 5000 之前的字节我已经按序收到了，下一段请从 5000 开始。

如果 `[5000,6000)` 丢了，但 `[6000,7000)` 先到了，接收方仍然只能回 `ACK=5000`，因为中间有洞。发送方连续收到相同 ACK，就能推测中间可能有段丢了。

## 超时重传：可靠性的兜底

发送方发出数据后会等待 ACK。如果超过 RTO 还没收到确认，就会重传。

```mermaid
sequenceDiagram
 participant S as 发送方
 participant R as 接收方

 S->>R: Seq=1000 数据段
 Note over S,R: 数据或 ACK 在网络中丢失
 Note over S: RTO 超时
 S->>R: 重传 Seq=1000
 R-->>S: ACK=下一期待序号
```

RTO 不能太小，也不能太大：

- 太小：只是网络抖动也会误重传，加重拥塞。
- 太大：真丢包时恢复慢，接口延迟升高。

现代 TCP 会根据 RTT 样本和平滑后的抖动动态计算 RTO。发生连续超时时，还会指数退避，避免网络已经拥塞时继续猛发。

RTO 的核心不是“固定等几秒”，而是基于 RTT 样本估算。常见思路是维护平滑 RTT 和 RTT 波动，再留出安全余量。对已经重传过的数据段，ACK 到底对应原始包还是重传包很难区分，因此这类样本通常不能直接拿来更新 RTT，否则 RTO 会被污染。

线上看到 RTO 增大或超时重传，不要立刻归因到应用慢。它可能来自丢包、队列拥塞、链路抖动、中间设备丢包、ACK 回程异常，也可能是接收方零窗口或系统调度抖动间接造成。

## 快速重传：不用等到超时

超时重传可靠但慢。快速重传利用重复 ACK 更早发现丢包。

假设接收方已经收到 0-999，期待 1000：

1. `[1000,2000)` 丢了。
2. `[2000,3000)`、`[3000,4000)`、`[4000,5000)` 先到。
3. 接收方因为 1000 这个洞没补上，只能连续回 `ACK=1000`。
4. 发送方收到 3 个重复 ACK，通常会认为 1000 之后的数据段丢了，于是提前重传。

3 个重复 ACK 是折中：一个重复 ACK 可能只是轻微乱序，不一定丢包；等 RTO 又太慢。

## SACK 和 D-SACK 解决什么？

普通 ACK 只能表达“某个序号之前都收到了”，不能表达“后面的哪些区间已经乱序收到了”。SACK 就是在 ACK 选项里补充这些区间。

例子：

- 发送方发 `[0,1000)`、`[1000,2000)`、`[2000,3000)`、`[3000,4000)`。
- `[1000,2000)` 丢了，后两段到了。
- 接收方累计 ACK 仍然是 `ACK=1000`，但 SACK 可以报告“我已经收到 `[2000,4000)`”。
- 发送方只需重传 `[1000,2000)`，不用把后面都重发。

D-SACK 则告诉发送方“某段数据我重复收到了”。它可以帮助判断 ACK 丢失、网络乱序、RTO 过小导致的误重传，但不能单独证明具体原因。

SACK 能不能用，通常在三次握手时协商。抓 `SYN` 和 `SYN+ACK` 时如果看到 `SACK permitted`，才说明双方后续可以使用 SACK。它减少的是无效重传，不是让网络不丢包。

Linux 相关参数：

```bash
sysctl net.ipv4.tcp_sack
sysctl net.ipv4.tcp_dsack
```

## 怎么用抓包识别重传？

先抓指定五元组或目标端口，避免无关流量太多：

```bash
tcpdump -i eth0 -nn -s 0 -w /tmp/tcp-retrans.pcap 'host <peer-ip> and tcp port <port>'
```

命令行快速看包时可以加 `-S` 显示真实序列号：

```bash
tcpdump -i eth0 -nn -S 'host <peer-ip> and tcp port <port>'
```

在 Wireshark 里常用这些过滤条件：

```text
tcp.analysis.retransmission
tcp.analysis.fast_retransmission
tcp.analysis.duplicate_ack
tcp.analysis.out_of_order
tcp.analysis.zero_window
```

判断时不要只看一个标签。比如 `duplicate_ack` 可能来自丢包，也可能来自乱序；`retransmission` 可能是真丢包，也可能是 ACK 丢失或 RTO 过小造成的误重传。要结合时间线、RTT、SACK/D-SACK、发送窗口、接收窗口一起看。

Linux 侧还可以看连接级和系统级指标：

```bash
ss -ti dst <peer-ip>
nstat -az | egrep 'TcpRetransSegs|TcpExtTCPSack|TcpExtTCPDSACK|TcpExtTCPTimeouts'
netstat -s | egrep -i 'retrans|SACK|timeout|segments retransmited'
```

这些指标多是累计值，排障时要间隔采样看增量，不能只看一次快照。

## 校验和是不是强一致性保证？

不是。TCP 校验和能发现常见传输错误，但它不是密码学完整性校验，也不能防恶意篡改。HTTPS/TLS 里的 AEAD、应用层 hash、存储校验，解决的是更强的完整性和安全问题。

所以“TCP 可靠”不要扩展成“绝对不会错”。它的含义是在协议能力范围内尽力做到有序、无重复、可靠交付；超出重试、连接中断、应用协议错误仍然需要上层处理。

## 上层还需要做什么？

TCP 解决的是单条连接里的字节流可靠交付，不负责业务语义。后端系统通常还要补这些能力：

| 场景                         | TCP 能做什么           | 应用还要做什么                     |
| ---------------------------- | ---------------------- | ---------------------------------- |
| 请求发出后连接断开           | 尽力传输已发送字节     | 判断结果未知，设计幂等和查询/补偿  |
| 服务端已处理但响应丢失       | 可能重传连接内字节     | 客户端重试必须带业务幂等键         |
| 长连接空闲太久被中间设备清理 | TCP 未必立刻感知       | 应用心跳、读写超时、连接池健康检查 |
| 数据被恶意篡改               | TCP 校验和不是安全机制 | TLS、签名、MAC 或应用层完整性校验  |

这也是为什么 RPC 框架和消息系统都会在 TCP 之上继续设计请求 ID、超时、重试、幂等、心跳和应用层协议格式。

## 小结

- TCP 可靠的是字节流，不是应用层消息边界。
- 序列号和 ACK 是可靠传输的底座，用来确认、排序、去重和重传。
- 超时重传是兜底，快速重传用重复 ACK 更快恢复丢包。
- SACK 让发送方知道哪些乱序区间已收到，减少无效重传；D-SACK 可辅助识别误重传。
- 抓重传要结合 tcpdump、Wireshark、`ss -ti` 和内核计数器增量，不能只凭一个标签下结论。
- TCP 校验和不是安全完整性保证，TLS 和应用层校验仍然有必要；业务成功还要靠超时、重试、幂等和补偿。

## 参考

基于 IETF RFC 793、RFC 9293、RFC 6298、RFC 2018、RFC 2883、Linux man-pages、tcpdump 文档和 Linux TCP 诊断计数器说明整理，并核对了 RTO、快速重传、SACK、D-SACK、校验和与应用层重试幂等的边界。
