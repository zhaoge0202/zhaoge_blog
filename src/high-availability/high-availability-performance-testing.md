---
title: "压测应该测什么？容量评估怎么做？"
description: "从压测分类、关键指标和容量推算流程讲清高可用方案如何验证。"
breadcrumb: true
article: true
editLink: false
category:
  - "高可用"
tag:
  - "进阶"
  - "项目实战"
  - "体系化"
prev:
  text: "容灾、多活、故障转移分别解决什么问题？"
  link: "/high-availability/high-availability-disaster-recovery.html"
next:
  text: "工具实践"
  link: "/tools/"
---

# 压测应该测什么？容量评估怎么做？

> 高可用设计如果不回到压测、容量和故障验证，很多时候只停留在“感觉应该能扛”。

很多系统设计题最后都会落到一句：

> “我们上线前会做压测。”

这句话本身没问题，但太空了。真正有价值的是继续往下说：

- 测哪些指标
- 用什么流量模型
- 怎么估算容量
- 怎么判断系统已经到拐点

因为压测不是为了跑一个好看的 `QPS` 数字，而是为了验证系统在目标流量下还能不能稳定工作。

## 先区分 4 种常见测试，不然很容易测偏

| 类型       | 核心目标                           |
| ---------- | ---------------------------------- |
| 性能测试   | 在目标指标下验证系统是否达标       |
| 负载测试   | 逐步加压，找到稳定上限             |
| 压力测试   | 继续越线，观察系统失稳和自愈行为   |
| 稳定性测试 | 长时间运行，暴露泄漏和资源积累问题 |

这四种测试经常被统称成“压测”，但它们关注点不同。

比如：

- 你要验证大促前是否能扛 5000 QPS，更像性能测试 / 负载测试
- 你要看限流和熔断会不会生效，更像压力测试
- 你要看 12 小时后连接池会不会漏光，更像稳定性测试

## 压测最该盯的指标，不是只有 QPS

至少要同时看这几类：

| 指标                     | 关注点               |
| ------------------------ | -------------------- |
| RT / P99 / P999          | 用户体感和长尾延迟   |
| QPS / TPS                | 吞吐能力             |
| 成功率                   | 错误是否开始放大     |
| 并发数                   | 当前系统负载水平     |
| CPU / 内存 / GC          | 资源是不是被顶满     |
| 线程池 / 连接池 / 队列   | 有没有局部资源先耗尽 |
| 数据库 / Redis / MQ 指标 | 真正瓶颈是不是在下游 |

平均 RT 经常最会骗人。

一个系统可能平均只要 80ms，但 P99 已经 1.5s 了。
对用户来说，真正难受的往往就是这 1% 的长尾请求。

## 为什么容量评估不能只靠“拍个日活系数”

很多粗略估算会这么做：

- `DAU × 某个经验系数 = 峰值 QPS`

这可以拿来做第一版粗估，但不能直接拿来当上线结论。

因为真正的峰值往往来自具体业务场景，比如：

- 整点开抢
- 秒杀开始
- 支付回调集中到达
- 大促某个会场流量爆发

所以容量评估更像一个分步骤过程。

## 一个更靠谱的容量评估顺序

### 第一步：先定业务场景

先别算，先定最重要的流量模型：

- 是读多写少，还是写多读多
- 峰值是不是出现在整点
- 哪条链路最关键
- 下游依赖有哪些

如果你测的是推荐接口，但真正的大促瓶颈在下单链路，那这个压测价值就很有限。

### 第二步：给出目标值

比如：

- 峰值 5000 QPS
- P99 小于 300ms
- 成功率不低于 99.95%

没有目标值，压测就很容易变成“压到哪算哪”。

### 第三步：用历史数据做初始估算

可以结合：

- 历史峰值
- 活动预期放大倍数
- 业务增长率
- 安全冗余

得到一个初始压测目标。

### 第四步：再用 Little's Law 做量级校验

在系统还处在线性区间时，可以用：

```text
并发数 ≈ QPS × RT
```

比如目标 `QPS = 5000`，目标平均 `RT = 100ms = 0.1s`：

```text
并发数 ≈ 5000 × 0.1 = 500
```

这说明系统至少得能稳定承载 500 个在途请求量级。

但这只是校验，不是最终答案。
因为一旦系统接近瓶颈，`RT` 会非线性上升，这个公式就不再好用了。

## 真正的拐点，通常看这几个信号

系统开始接近极限时，常见现象有：

- QPS 不再明显增长
- P99 / P999 突然陡升
- 线程池队列开始堆积
- 连接池等待时间明显升高
- 数据库慢查询变多
- Redis、MQ、RPC 下游延迟同步升高

这时候就不是“还能不能再多压一点”的问题，而是：

**系统已经离线性工作区不远了。**

## 压力测试不只是看“什么时候挂”，还要看“挂成什么样”

真正有高可用意识的团队，不会只测崩溃点，还会看：

- 超过阈值后有没有返回 `429` / `503`
- 限流是否生效
- 熔断是否打开
- 队列是否有背压
- 流量回落后系统能不能自动恢复

也就是说，压力测试不仅看“会不会挂”，还看“挂的时候有没有优雅失败”。

## 稳定性测试最常暴露的是积累型问题

这类问题短压 5 分钟经常看不出来，但长压几个小时会暴露：

- 内存泄漏
- 连接泄漏
- 线程数持续上涨
- Full GC 越来越频繁
- MQ 积压慢慢爬高

所以压测如果只看短时间吞吐，很容易漏掉真正的线上风险。

## 一个很实用的压测清单

如果要把答案讲得更像工程实践，可以按这张清单说：

1. 明确业务场景和目标指标。
2. 用历史流量和活动预估确定压测目标。
3. 构造接近真实的数据量、热点分布和读写比例。
4. 逐步加压，观察 RT、成功率、线程池、连接池、数据库和缓存指标。
5. 超过阈值后继续压，看限流、熔断、降级和恢复是否符合预期。
6. 长时间跑稳定性测试，找泄漏、堆积和慢性资源问题。

这套说下来，面试官基本能感受到你知道压测不是“跑 JMeter 点开始”。

## 容易踩的坑

### 只测单接口，不测完整链路

单接口 1 万 QPS 很漂亮，但真实链路里还有：

- 数据库
- Redis
- MQ
- RPC 下游

真正扛不住的往往不是接口本身。

### 用空数据或小数据压

真实线上很多问题来自：

- 大表
- 热点 key
- 深分页
- 大对象序列化

如果压测数据太干净，结果会偏乐观。

### 只看平均值，不看长尾

平均 RT 好看，不能代表高峰期的真实体感。

### 只看吞吐，不看保护动作

高可用不是单纯追 QPS，还要验证限流、熔断、降级、自愈这些策略是不是按预期工作。

## 小结

- 压测不是为了跑一个漂亮数字，而是为了验证系统在目标流量下是否还能稳定工作。
- 性能测试、负载测试、压力测试、稳定性测试关注点不同，别混成一个概念。
- 容量评估先看业务场景和历史流量，再用公式做量级校验，最后一定要靠真实压测确认。
- 到达瓶颈时最值得看的不是平均 RT，而是 P99、资源耗尽点和保护动作是否生效。
- 真正的高可用验证，不只看“能跑多快”，还看“过载时会不会优雅失败、回落后能不能恢复”。

## 参考

综合自社区高可用资料，并按“容量评估 +
保护动作验证”的主线做了重写整理。
