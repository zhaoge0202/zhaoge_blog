spring:
  datasource:
    url: jdbc:h2:mem:interview_platform;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  flyway:
    enabled: false

app:
  bootstrap-admin:
    enabled: false
  jwt:
    secret: test-secret-change-this-secret-to-at-least-32-characters
    ttl-minutes: 60
